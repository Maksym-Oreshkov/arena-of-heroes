#!/usr/bin/env bash
# Minimal shim so tools looking for `python` run python3
if command -v python3 >/dev/null 2>&1; then
  exec python3 "$@"
else
  echo "python3 not found; please install Python 3 (e.g., brew install python)" >&2
  exit 1
fi

